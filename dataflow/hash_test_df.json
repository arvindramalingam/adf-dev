{
	"name": "hash_test_df",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"linkedService": {
						"referenceName": "AzureSqlDatabase1",
						"type": "LinkedServiceReference"
					},
					"name": "source1"
				}
			],
			"sinks": [
				{
					"dataset": {
						"referenceName": "S3_DESTINATION",
						"type": "DatasetReference"
					},
					"name": "sink1"
				}
			],
			"transformations": [
				{
					"name": "derivedColumn1"
				}
			],
			"scriptLines": [
				"parameters{",
				"     sapKeyColumns as string[] ([\"StudentName\",\"City\",\"Pincode\"])",
				"}",
				"source(output(",
				"          StudentName as string,",
				"          City as string,",
				"          Pincode as integer,",
				"          Dob as date",
				"     ),",
				"     allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     format: 'table',",
				"     store: 'sqlserver',",
				"     schemaName: 'dbo',",
				"     tableName: 'Student',",
				"     isolationLevel: 'READ_UNCOMMITTED') ~> source1",
				"source1 derive(hashinput = reduce(array(toString(byNames($sapKeyColumns))), '', #acc + iif(or( and(length(#acc) > 0, not(and(length(#acc)==3, right(#acc, 3) == '|_|')) ),and(not(length(#acc) > 0), isNull(#item)) ), '|_|', '') + iif(isNull(#item), '', toString(#item)), #result),",
				"          hashkey = sha2(256, reduce(array(toString(byNames($sapKeyColumns))), '', #acc + iif(or( and(length(#acc) > 0, not(and(length(#acc)==3, right(#acc, 3) == '|_|')) ),and(not(length(#acc) > 0), isNull(#item)) ), '|_|', '') + iif(isNull(#item), '', toString(#item)), #result)),",
				"          testinput = reduce(array(toString(byNames($sapKeyColumns))), '', #acc + iif(or(or( and(length(#acc) > 0, not(and(length(#acc)==3, right(#acc, 3) == '|_|')) ),and(not(length(#acc) > 0), isNull(#item))),and(length(#acc) > 0, not(and(length(#acc) == 6, right(#acc, 6) == '|_||_|')) ) ), '|_|', '') + iif(isNull(#item), '', toString(#item)), #result),",
				"          TestNull = mapIndex(byNames($sapKeyColumns), if(isNull(byNames[i]), 'DefaultValue', byNames[i]))) ~> derivedColumn1",
				"derivedColumn1 sink(allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     input(",
				"          Name as string,",
				"          Age as string,",
				"          Sex as string,",
				"          Qualification as string",
				"     ),",
				"     format: 'parquet',",
				"     umask: 0022,",
				"     preCommands: [],",
				"     postCommands: [],",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true) ~> sink1"
			]
		}
	}
}